// settings.gradle

pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS) // This line is the key
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "ElectricityBillEstimator" // Your project name
include ':app' // Your app module